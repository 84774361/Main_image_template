document.addEventListener("DOMContentLoaded", () => {
  const summary = document.getElementById("summary");
  if (summary) {
    summary.textContent = "Plugin shell loaded. Batch logic is temporarily disabled for load testing.";
  }
});
